import { ShowMessage } from './show-message';

describe('ShowMessage', () => {
  it('should create an instance', () => {
    const directive = new ShowMessage();
    expect(directive).toBeTruthy();
  });
});
